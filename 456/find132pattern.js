/**
 * @param {number[]} nums
 * @return {boolean}
 */
var find132pattern = function (nums) {
    let index = 0;
    let i = nums[0];
    while (index < nums.length) {
        
        index++;
        console.log(index);
    }

    return false;
};

let nums = [3, 0, 1, 2];
console.log(find132pattern(nums));
